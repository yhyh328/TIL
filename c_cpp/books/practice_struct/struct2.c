#include <stdio.h>
#include "my_header.h"

void main() 
{
    for (int i = 0; i < 10; i++)
    {
        printf("No: %d, Name: %s, Age: %d\n", 
               arr[i].no, arr[i].name, arr[i].age);
    }
}